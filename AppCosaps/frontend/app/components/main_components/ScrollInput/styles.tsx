import { StyleSheet, Dimensions } from "react-native";
import { Colors } from "@/app/MainStyle";

const styles = StyleSheet.create({
  container: {
    paddingVertical: 1,
  },
  text: {
    color: Colors.Fundo_Claro_2,
    fontSize: 22,
    fontWeight: "bold",
    paddingVertical: 0.1,
  },
});

export default styles;
