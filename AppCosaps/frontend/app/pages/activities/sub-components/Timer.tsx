import { TimerProps } from "@/app/interfaces/timer/timer_props";
import { View } from "react-native";

const Timer: React.FC<TimerProps> = ({ target_time, set_number, onComplete }) => {
  return (
  <View>
    
  </View>)
};

export default Timer;